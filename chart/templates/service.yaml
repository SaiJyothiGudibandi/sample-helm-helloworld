apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.name}}-service"
  labels:
    app: {{ .Values.name}}
spec:
  type: {{ .Values.service.type}}
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.name}}
