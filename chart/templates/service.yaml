apiVersion: v1
kind: Service
metadata:
  name: sg-helm-hello-server
  labels:
    app: sg-helm-hello
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: sg-helm-hello
